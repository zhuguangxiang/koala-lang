
package test;

import "koala/io";
import "github.com/nio";    // error: load failed
import "koala/io";          // error: load duplicated
import lang2 "koala/lang";  // error: unused import

var array []string = nil;
var greeting = string("hello, world");
var good lang.String = "good luck";
const ARRAY_SIZE = 4096;
var a, b int = 100, 100;
var good = "good luck";     // error: redefined

var t lang.Tuple;

func Main(args []string) {
  io.Print("hello, world\n");
}

func Sub(a int, b int) int {
  //100 = 200;    // error: cannot assign to 100
  return a - b;
}

func Add(a int, b int) (int) {
  var r int = a + b;
  t.Get(100);
  return r;
}
