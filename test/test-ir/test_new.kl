// SKIP: koalac %s --irgen --dump=no-opt-ir | filecheck %s

// class Point {
//     var x int
//     var y int

//     func __init__(_x int, _y int) {
//         x = _x
//         y = _y
//     }

//     func __str__() str {
//         return "Point()"
//     }
// }

func main() {
    let r = range(1, 10)
    let p = Point(1, 2)
    let s = str("hello")
    print(r)
}

// CHECK-LABEL: func @main() <NO-TYPE> {
// CHECK: %bb0(entry):
// CHECK: %0 = new @range, 1 int64, 10 int64
// CHECK: %r = local [immutable] range
// CHECK: move %r range, %0 range
// CHECK: %1 = new @Point, 1 int64, 2 int64
// CHECK: %p = local [immutable] Point
// CHECK: move %p Point, %1 Point
// CHECK: ret void
// CHECK: }
