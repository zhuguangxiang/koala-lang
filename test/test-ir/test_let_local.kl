// SKIP: koalac %s --opt --dump=ir | filecheck %s

func bar() str { return "hello" }

func foo() int {
    let i = 10
    let j = i + 1

    let l = [1, 2, 3]

    let l2 = list(1,2,30)

    let v = int(100)

    let t = tuple(10, 20)

    let t2 = (10, 20, 30)

    let ret = bar()

    var x = 10

    x + 1

    return j
}

// CHECK-LABEL: --- IR Dump After dead-code-elimination (Iter 1) [@foo] ---
// CHECK-LABEL: func @foo() int64
// CHECK: %0 = call @bar
// CHECK-NOT: local
// CHECK-NOT: move
// CHECK: ret 11 int64
