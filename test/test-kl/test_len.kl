// RUN: koalac %s 2>&1 | filecheck %s

class Foo {
    func len() str {
        return "hello"
    }
}

func main() {
    let n = len(200)
    let n2 = len(Foo())
}

// CHECK: object of type 'j' has no len()
// CHECK: 'Ltest_len.Foo;.len' must return int for len() call
