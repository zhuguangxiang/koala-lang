// RUN: koalac %s | filecheck %s

func bar(msg str?) {
    let n = msg == 10
}

func foo() {
    let n = nil

}

// CHECK: cannot compare optional type with non-optional type for '==' operator.
// CHECK: cannot assign 'nil' to variable 'n' without explicit type
