import os

# Variables substituted by CMake configure_file()

config.src_root = "@PROJECT_SOURCE_DIR@"
config.build_root = "@PROJECT_BINARY_DIR@"
config.cmake_path = "@CMAKE_COMMAND@"
config.koala_path = "@KOALA_PATH_FOR_LIT@"

# Load the real test configuration.
lit_config.load_config(
    config, os.path.join(config.src_root, "test", "lit.cfg"))
