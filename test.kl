
let s = "hello
let "

let abc = (100)

let size = sizeof(s)

let arr [Int; 32] = [1,2,3]

let i UInt8 = 0
let j uint8 = 0
let k u8 = 0

/// let def = 200*1

// var2 abc Int32

class A {
    func add<T : Person<Person<T>>>() T {

    }
}

struct Pointer {
    var s Pointer<UInt8>
    var len UInt32
}

/*
func add(a Int, b Int) {
    let c = a >> 10 + b << 20 - 1 > 2 + 1 < Person<B> > 2;
    let p Person<Cat<Int32>, Cat> = io.Person<Cat<T>, Person<T>>();
    let p	= Cat<T>()
}
*/
