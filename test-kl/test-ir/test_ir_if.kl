// RUN: koalac %s --opt --dump=opt-ir | filecheck %s

let i = 10
// TODO: foward reference
// let j2 = j + 2
let j = i + 1
let b = j > 10

if j > 10 {
    var k = j + 2
    print(j, k)
} else {
    print(i)
}

// TODO:
// let k int? = 10
// if k != null {
//     print(k)
// } else {
//     print("k is null")
// }

func foo() {
    let i = 10
    let j = i + 1
    let k = j + 3
    print(k)
    let b = j > 10

    if j > 10 {
        var k = j + 2
        print(j, k)
    } else {
        print(i)
    }

    // TODO:
    // let a1 = 10
    // let b1 = 20
    // let my_list = list(a1, b1)  // (1) 初始扫描到这里，a 和 b 已经是常量了
    // let x = my_list[0]        // (2) 依赖 my_list 的折叠结果
}

// CHECK-LABEL: --- IR Dump After dead-code-elimination (Iter 2) [@__init__] ---
// CHECK-LABEL: func @__init__()
// CHECK: set_global @i int64, 10 int64
// CHECK: set_global @j int64, 11 int64
// CHECK: set_global @b bool, true bool
// CHECK: call @print [ext = true, path = 'std/builtin'], 11 int64, 13 int64
// CHECK: ret void

// CHECK-LABEL: --- IR Dump After dead-code-elimination (Iter 2) [@foo] ---
// CHECK-LABEL: func @foo()
// CHECK: call @print [ext = true, path = 'std/builtin'], 14 int64
// CHECK: call @print [ext = true, path = 'std/builtin'], 11 int64, 13 int64
// CHECK: ret void
