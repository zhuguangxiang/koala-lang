// RUN: koalac %s 2>&1 | filecheck %s

let l = list[int]()
l.append(1)
l.append(2.0)
// CHECK: argument type is not compatible.

l[0] + 1

l[0] = 3

l[0] = true
// CHECK: argument type is not compatible.

l["ok"] = true
// CHECK: argument type is not compatible.
// CHECK: argument type is not compatible.

let l2 = list[list[int]]()
l2.append(100)
// CHECK: argument type is not compatible.

l2[0][1] = 200

let l3 = [1, 2, 3]

let l4 = [1, 2.0, "hi"]
l4.append(3)
l4.append("hello")

let l5 = []
l5.append(1)
l5.append(2.0)
l5.append("hi")

let x = l5[0].as(int) + 1

// 123.as(any)
// 456.as(Number[int])

// 100.is(int)
// 200.is(float)

// let l6 = list[int](1, 2.1)

// let l7 = list(1,2,3)

// let l8 = list[int]()

// CHECK-NOT: {{.}}
