# Koala Standard Libraries

## `builtin` module

The `builtin` module is one of core modules of koala language. Most of them in `builtin` module are native c implementations. This module is imported automatically.

### builtin types

### builtin functions

## modules

### `sys` module

### `io` module

### `assert` module

### `unittest` module
