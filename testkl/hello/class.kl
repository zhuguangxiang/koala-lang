
package hello

import "io"

/*
class Person {
  name string = "LiSi"
  age int = 200
  //dog Dog<string> = Dog()

  func greeting(msg string) {
    io.puts(msg)
    io.puts(name)
    name = msg;
    age = 100
    io.puts(name)
    //dog.set_name("ErHa");
    //io.puts(dog.get_name());
  }
}
*/

class Dog<T> {
  name T

  func __init__() {
    io.puts("dog() called");
  }

  func set_name(n T) {
    name = n
  }

  func get_name() T {
    return name
  }
}
