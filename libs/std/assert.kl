// std/assert.kl — Koala standard assertion library
// Used by `koala test` to validate program correctness.

// Basic assertion: fails if condition is false.

func assert(cond bool) {
    if !cond {
        panic("assertion failed")
    }
}

// func assert(cond bool, msg str? = null) {
//     if cond == false {
//         if msg == null {
//             panic("assertion failed")
//         } else {
//             panic("assertion failed: " + msg)
//         }
//     }
// }

// // Assert that two values are equal.
// func assert_eq(a, b) {
//     if a != b {
//         panic("assertion failed: expected " + str(a) + " == " + str(b))
//     }
// }

// // Assert that two values are not equal.
// func assert_ne(a, b) {
//     if a == b {
//         panic("assertion failed: expected " + str(a) + " != " + str(b))
//     }
// }

// // Assert that a < b.
// func assert_lt(a, b) {
//     if !(a < b) {
//         panic("assertion failed: expected " + str(a) + " < " + str(b))
//     }
// }

// // Assert that a > b.
// func assert_gt(a, b) {
//     if !(a > b) {
//         panic("assertion failed: expected " + str(a) + " > " + str(b))
//     }
// }

// // Assert that two numbers are close within epsilon.
// func assert_close(a, b, eps = 1e-9) {
//     if abs(a - b) > eps {
//         panic("assertion failed: expected " + str(a) + " ≈ " + str(b) +
//               ", diff=" + str(abs(a - b)) + ", eps=" + str(eps))
//     }
// }
