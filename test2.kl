
var i = 100

x?.foo()?.bar()!.baz()![1]?.qux()
x + y!
(x + y)!(100)

if x.is(int) {

}

if y.as(str) {

}

if y = z.as(float) {
}

if x = y {

}

if x != null {

}

pub trait Fly {

}

pub trait Animal[T] {

}

pub class Bird : Fly & Animal[Bird] {

}

pub trait Seq2[T : Fly & Animal[T], V] : Fly & Animal[T] {
    func __concat__(seq Seq2[T, int]) int
}

type FUNC_A = func(int, str) float?;
var f FUNC_A? = null;

func hello(callback FUNC_A) {
    var res = callback(10, "test")
    if res != null {
        print(res)
    }
}

hello(func(a int, b str) float? {
    return 3.14
})

func world(f func(int, str) float?) {
    if f != null {
        var res = f(20, "world")
        if res != null {
            print(res)
        }
    }
}

// func main() {
//     print("hello, world")
// }
